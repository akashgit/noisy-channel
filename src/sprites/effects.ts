// Effect/particle sprites — filled in by sprite-artist agent

export interface EffectChar {
  char: string;
  color: string;
}

export const confettiChars: EffectChar[] = [];
export const sparkChars: EffectChar[] = [];
export const sparkleChars: EffectChar[] = [];
